{
  "name": "vaultvista",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev:api": "pnpm --filter @vaultvista/api dev",
    "dev:web": "pnpm --filter @vaultvista/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:integration": "pnpm --filter @vaultvista/api test:integration",
    "test:e2e": "pnpm --filter @vaultvista/e2e test",
    "db:generate": "pnpm --filter @vaultvista/db generate",
    "db:migrate": "pnpm --filter @vaultvista/db migrate"
  }
}
